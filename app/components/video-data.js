import Ember from 'ember';

export default Ember.Component.extend({
  // getVideo() {
  //   $.getJSON('/video', {id: video.id}).then(data => {
  //     this.set('video', data);
  //   });
  // }
});
