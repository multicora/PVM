{{upload-video isShown=isShownUploadPopup}}
{{video-record isShown=isShownRecordPopup}}
{{watch-buttons isShownRecordPopup=isShownRecordPopup isShownUploadPopup=isShownUploadPopup}}
{{library-content-videos-container 
    isShownUploadPopup=isShownUploadPopup
    isShownRecordPopup=isShownRecordPopup
    videos=videos
    isShownSendPopup=isShownSendPopup
}}
