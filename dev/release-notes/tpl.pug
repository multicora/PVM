.release-notes-page
  .mdl-grid
    h4 Release notes
    .mdl-grid.wrapper
      .container(ng-repeat="release in vm.releases")
        h6 {{release.date}} &nbsp Bizkonect App Version {{release.version}}
        ul
          li(ng-repeat="note in release.notes") {{note}}