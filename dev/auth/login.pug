.vertical-aligned-middle.auth.full-page
  .auth-container
    img.logo(src='/files/images/logo_bizkonect.svg')
    h2.title Login
    form.login-form(layout="column")
      md-input-container
        label Login
        input(
          ng-model="login"
          required)
      md-input-container
        label Password
        input(
          type="password"
          ng-model="password"
          required)
      p.error(ng-if="vm.errorMessage") Login failed: {{vm.errorMessage}}

      .button-container
        a.pointer(ng-click="redirect('/register')") Register
        a.pointer(ng-click="redirect('/reset-password')") Forgot password
        button.mdl-button.mdl-js-button.mdl-button--raised.mdl-js-ripple-effect.mdl-button--colored(
          ng-click="vm.authenticate(login, password)")
          | login